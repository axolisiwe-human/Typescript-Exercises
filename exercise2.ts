type statements = string | boolean //Create a type alias for a union type that can be either a string or a boolean.
let statement : statements //statements becomes a data type of string and boolean combined
statement = true  //statement is a variable with statements as its data type
statement = 'true'
